<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.travelplannerapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".launcher.LauncherActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signin.SignInActivity" />

        <activity android:name=".signup.SignUpActivity" />

        <activity android:name=".travels.TravelsActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>

        <activity android:name=".transport.TransportActivity" />

        <activity android:name=".tickets.TicketsActivity" />

        <activity android:name=".scanner.ScannerActivity" />

        <activity android:name=".accommodation.AccommodationActivity" />

        <activity android:name=".dayplans.DayPlansActivity" />

        <activity android:name=".dayplans.addplan.AddPlanActivity" />

        <activity android:name=".traveldetails.TravelDetailsActivity" />

        <activity
            android:name="com.github.travelplannerapp.dayplans.searchelement.SearchElementActivity"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <meta-data
            android:name="com.here.android.maps.appid"
            android:value="FcxYwwtlgIIwY1awmIax" />

        <meta-data
            android:name="com.here.android.maps.apptoken"
            android:value="1pBsuZwrgZBAeyHBpy5XIA" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.github.travelplannerapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="com.github.travelplannerapp.dayplans.searchelement.CitySuggestionProvider"
            android:authorities="com.github.travelplannerapp.dayplans.searchelement.CitySuggestionProvider"
            android:exported="false"
            android:grantUriPermissions="true">
        </provider>
    </application>
</manifest>
